import { MapBox } from "../../components/world";

import { Box } from "@chakra-ui/react";

const WorldPage = () => {
  return (
    <Box>
      <MapBox />
    </Box>
  );
};

export default WorldPage;
